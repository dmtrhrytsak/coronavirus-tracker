import styled from 'styled-components';

import { SearchBar } from './components/SearchBar';

const StyledApp: any = styled.div``;

StyledApp.SearchBar = styled(SearchBar)`
  margin-bottom: 18px;
`;

export default StyledApp;
